# syntax=docker/dockerfile:1
FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim
WORKDIR /app
COPY shared/ ./shared/
COPY services/tenant-service/ ./services/tenant-service/
WORKDIR /app/services/tenant-service
RUN uv sync --frozen
CMD ["sh", "-c", "uv run alembic upgrade head && exec uv run uvicorn app.main:app --host 0.0.0.0 --port 8002 --reload --reload-dir /app/services/tenant-service/app --reload-dir /app/shared/src"]
